# Dependências do scraper, além do requirements.txt + requirements-local.txt
# (MetaTrader5) já documentados na raiz do repo pro modo "MetaTrader 5"
# local. Instale os três nesta VM Windows:
#
#   pip install -r requirements.txt
#   pip install -r requirements-local.txt
#   pip install -r scraper/requirements.txt
#
requests>=2.31

# O gate de pregão usa zoneinfo.ZoneInfo("America/Sao_Paulo"). No Linux o
# zoneinfo lê a base de fusos do sistema, mas o Windows não tem uma — sem este
# pacote o scraper morre no start com ZoneInfoNotFoundError.
tzdata>=2024.1
